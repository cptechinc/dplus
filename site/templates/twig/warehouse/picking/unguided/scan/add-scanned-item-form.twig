<form action="{{ page.fullURL.getUrl() }}" method="post" id="scanned-item-form">
	<input type="hidden" name="action" value="add-lotserial">
	<input type="hidden" name="scan" value="{{ scan }}">

	<div class="form-group">
		<label for="itemID">Item ID</label>
		<input type="text" class="form-control-plaintext" name="itemID" id="itemID" value="{{ item.itemid }}" readonly>
	</div>

	<div class="row {{ item.is_item_normal ? 'collapse' : '' }}">
		<div class="col-sm-6">
			<div class="form-group">
				<label for="lotserial">Lot / Serial #</label>
				<input type="text" class="form-control" name="lotserial" id="lotserial" value="{{ item.lotserial }}">
			</div>
		</div>
		<div class="col-sm-6">
			<div class="form-group">
				<label for="lotserialref">Lot / Serial Ref</label>
				<input type="text" class="form-control" name="lotserialref" id="lotserialref" value="{{ item.lotserialref }}">
			</div>
		</div>
	</div>

	<div class="row">
		<div class="col-sm-6">
			<div class="form-group">
				<label for="binID">Bin ID</label>
				<input type="text" class="form-control-plaintext" name="binID" id="binID" value="{{ item.bin }}" readonly>
			</div>
		</div>
		<div class="col-sm-6">
			<div class="form-group">
				<label for="qty">Qty</label>
				<input type="number" class="form-control" name="qty" id="qty" max="{{ item.qty }}" value="">
				<small class="form-text text-muted">Bin Qty: {{ item.qty }}</small>
			</div>
		</div>
	</div>

	<div class="form-group">
		<button type="submit" class="btn btn-success">
			<i class="fa fa-plus" aria-hidden="true"></i> Add
		</button>
		&nbsp; &nbsp;
		<a href="{{ page.get_removescanURL() }}" class="btn btn-warning">Retry Search</a>
	</div>
</form>
