<div class="mt-4">
	<h3>Packed Serial #s</h3>

	<table class="table">
		<tr>
			<th>Carton #</th>
			<th>Serial #</th>
			<th>Remove</th>
		</tr>
		{% for item in packed_items %}
			<tr>
				<td>{{ item.carton }}</td>
				<td>{{ item.lotserial }}</td>
				<td>
					<form action="{{ page.fullURL.getUrl() }}" method="post">
						<input type="hidden" name="action" value="delete-barcode">
						<input type="hidden" name="recordnbr" value="{{ item.recordnumber }}">
						<input type="hidden" name="barcode" value="{{ item.lotserial }}">
						<button type="submit" class="btn btn-danger btn-sm" type="button">
							<i class="fa fa-trash" aria-hidden="true"></i> Remove
						</button>
					</form>
				</td>
			</tr>
		{% endfor %}
	</table>
</div>
