<table class="table table-sm">
	<thead>
		<tr>
			<th>Date</th>
			<th>Amount</th>
		</tr>
	</thead>
	<tbody>
		{% for booking in bookings %}
			<tr>
				<td>{{ booking.bookdate|convertdate('F Y') }}</td>
				<td>$ {{ booking.amount|currency }}</td>
			</tr>
		{% endfor %}
	</tbody>
</table>
