<form action="{{ input.url() }}" class="allow-enterkey-submit">
	<label for="invnbr"><strong>AP Invoice Number</strong></label> &nbsp; &nbsp;

	<div class="form-inline form-group">
		{% include 'util/bootstrap/form/input-group.twig' with {
				'inputgroup': {
					'type': 'prepend',
					'input': {
						'name': 'invnbr',
						'value': '',
						'attributes': {
							'placeholder': '',
							'aria-label':  'AP Invoice Number'
						}
					},
					'button': {
						'colorclass': 'btn-secondary',
						'text': 'fa fa-search'|icon,
						'attributes': {
							'title': 'Search AP Invoices',
							'data-toggle': 'modal',
							'data-target': '#ajax-modal',
							'data-lookupurl': page.searchLookupUrl('purchase-orders'),
							'data-input': '#invnbr'
						}
					}
				}
			}
		%}
	</div>
	<button type="submit" class="btn btn-success">
		<i class="fa fa-search" aria-hidden="true"></i> Find
	</button>
</form>
