<div class="card bg-transparent" role="alert">
	<div class="container">
		<div class="row">
			<div class="col-1 d-flex justify-content-center align-items-center bg-{{ type }} text-white">
				<span data-notify="icon"><i class="{{ iconclass }}" aria-hidden="true"></i></span>
			</div>
			<div class="col-11 alert-{{ type }} pt-1 pb-1">
				<span data-notify="title" class="h5">{{ title }}</span>
				<div class="notify-message">{{ message }}</div>
				<div class="notify-message">
					{% if fields %}
						Please Review the following fields:
						<ul class="list-inline comma-list mb-0">
							{% for id, description in fields %}
								<li class="list-inline-item"><a href="#{{ id }}" class="link">{{ description }}</a></li>
							{% endfor %}
						</ul>
					{% endif %}
				</div>
			</div>
		</div>
	</div>
</div>
