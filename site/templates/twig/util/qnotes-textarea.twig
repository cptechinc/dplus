<textarea name="{{ name ? name : 'note' }}" id="{{ id ? id : 'note' }}" wrap="hard" class="form-control w-auto qnotes" rows="{{ rows ? rows : 10 }}" cols="{{ cols }}" {{ readonly ? 'readonly' : '' }}>{{ val }}</textarea>
