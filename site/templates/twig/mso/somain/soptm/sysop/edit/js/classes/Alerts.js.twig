class SoptmAlerts extends CodeAlerts {
	static instance = null;

	static getInstance() {
		if (this.instance === null) {
			this.instance = new SoptmAlerts();
		}
		return this.instance;
	}

	codeExists(code, callback) {
		swal2.fire({
			title: 'Code Exists',
			text: 'Edit Code ' + code + '?',
			icon: 'question',
			showCancelButton: true,
			confirmButtonText: 'Yes'
		}).then(function (result) {
			if (result.value) {
				callback(true);
				return true;
			}
			callback(false);
			return false;
		});
	}
}
