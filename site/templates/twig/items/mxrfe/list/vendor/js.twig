$(function() {
	{% if input.get.offsetExists('focus') and input.get.string('focus') != 'new' %}
		// $('tr[data-mxrfe="{{ input.get.string('focus') }}"]').addClass('highlight');
		// $('html, body').animate({scrollTop: $('#{{ page.focus }}').offset().top,},700,'linear');
		// $('html, body').animate({scrollTop: $('[data-mxrfe="{{ page.focus }}"]').offset().top,},700,'linear');
	{% endif %}

/* =============================================================
	Lookup Modal Functions
============================================================= */
	var modal_ajax = $('#ajax-modal');
});
