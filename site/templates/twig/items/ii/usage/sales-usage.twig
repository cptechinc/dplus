<table class="table table-striped table-sm">
	<thead>
		{% for column in json['columns']['sales usage'] %}
			<th class="{{ module_json.get_justifyclass(column['headingjustify']) }}">
				{{ column['heading'] }}
			</th>
		{% endfor %}
	</thead>
	<tbody>
		{% for salesusage in json['data']['sales usage'] %}
			<tr>
				{% for column, properties in json['columns']['sales usage'] %}
					<td class="{{ module_json.get_justifyclass(properties['datajustify']) }}">
						{{ salesusage[column] }}
					</td>
				{% endfor %}
			</tr>
		{% endfor %}
	</tbody>
</table>
