<table class="table table-striped table-sm">
	<thead>
		{% for column in json['columns']['24month'] %}
			<th class="{{ module_json.get_justifyclass(column['headingjustify']) }}">
				{{ column['heading'] }}
			</th>
		{% endfor %}
	</thead>
	<tbody>
		{% for month in warehouse['months'] %}
			<tr>
				{% for column, properties in json['columns']['24month'] %}
					<td class="{{ module_json.get_justifyclass(properties['datajustify']) }}">
						{{ month[column] }}
					</td>
				{% endfor %}
			</tr>
		{% endfor %}
	</tbody>
</table>
