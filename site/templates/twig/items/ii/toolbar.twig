<nav class="toolbar-sidebar">
	<ul class="nav">
		{% for child in page.children_user_allowed() %}
			<li>
				<a href="{{ child.url_itemID(item.itemid) }}">
					{{ child.title }}
				</a>
			</li>
		{% endfor %}
	</ul>
</nav>
