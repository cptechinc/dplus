<h4>Kits</h4>
<table class="table table-striped table-sm">
	<thead>
		{% for column in json['columns']['kit'] %}
			<th class="{{ module_json.get_justifyclass(column['headingjustify']) }}">
				{{ column['heading'] }}
			</th>
		{% endfor %}
	</thead>
	<tbody>
		{% for kit in json['data']['kit'] %}
			<tr>
				{% for column, properties in json['columns']['kit'] %}
					<td class="{{ module_json.get_justifyclass(properties['datajustify']) }}">
						{{ kit[column] }}
					</td>
				{% endfor %}
			</tr>
		{% endfor %}
	</tbody>
</table>
