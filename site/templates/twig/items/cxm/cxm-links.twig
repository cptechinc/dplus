<div class="mb-3">
	&nbsp;
	<a href="{{ page.cxmURL(page.custID) }}" class="btn btn-primary">
		<i class="fa fa-arrows" aria-hidden="true"></i> CXM
	</a>

	{% if page.has('custitemID') %}
		<a href="{{ page.cxm_customerURL(page.custID) }}" class="btn btn-primary"> View Customer {{ page.custID }}</a>
	{% endif %}
</div>
