{#
	var                    Description / Instance of
	-------------------------------------------------
	page                   ProcessWire\Page
	kitm                   module Kim
	kitm.recordlocker      module RecordLockerUserFunction
	item                   ItemMasterItem
	kit                    InvKit
#}
<table class="table table-sm">
	<thead>
		<tr class="bg-dark text-white p-3">
			<th>Component</th>
			<th>Tag</th>
			<th>Description</th>
			<th class="text-right">Rate</th>
			<th></th>
		</tr>
	</thead>
	<tbody>
		{% for component in kit.components %}
			<tr>
				<td>{{ component.itemid }}</td>
				<td>{{ component.usagetag }}</td>
				<td>{{ component.item.description }}</td>
				<td class="text-right">{{ component.usage|number_format }}</td>
				<td class="text-center">
					{% if kitm.recordlocker.function_locked(kit.itemid) == false or kitm.recordlocker.function_locked_by_user(kit.itemid) %}
						<a href="" class="btn btn-sm btn-warning">
							<i class="fa fa-pencil" aria-hidden="true"></i> Edit
						</a>
						&nbsp;
						<a href="" class="btn btn-sm btn-danger">
							<i class="fa fa-trash" aria-hidden="true"></i> Delete
						</a>
					{% else %}
						<span class="badge badge-secondary">
							<i class="fa fa-lock" aria-hidden="true"></i> Locked By {{ recordlocker.get_locked_user('vxm', vxm.get_recordlocker_key(item)) }}
						</span>
					{% endif %}
				</td>
			</tr>
		{% endfor %}
	</tbody>
</table>
