<form action="{{ page.wire('input').url() }}" method="post">
	<input type="hidden" name="action" value="update">
	<input type="hidden" name="itemID" value="{{ dimensions.itemid }}">

	<div class="row">
		<div class="col-sm-6">
			{% include 'items/itm/dimensions/form/standard-packaging.twig' %}
		</div>
		<div class="col-sm-6">
			{% include 'items/itm/dimensions/form/dimensions.twig' %}
		</div>
	</div>
	<div class="d-flex justify-content-between align-items-center">
		<button type="submit" class="btn btn-success">
			<i class="fa fa-floppy-o" aria-hidden="true"></i> Save
		</button>
		<a href="{{ page.itmUrl(dimensions.itemid) }}" class="btn btn-warning">
			<i class="fa fa-times" aria-hidden="true"></i> Close
		</a>
	</div>
</form>
