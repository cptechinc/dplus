<div class="modal fade" id="notes-inspection-modal" tabindex="-1" role="dialog" aria-labelledby="notes-inspection-modal-label" aria-hidden="true">
	<div class="modal-dialog modal-lg" role="document">
		<div class="modal-content">
			<div class="modal-header">
				<h5 class="modal-title" id="notes-inspection-modal-label"><span class="action-desc">Adding</span> {{ item.itemid }} Inspection Notes</h5>
				<button type="button" class="close" data-dismiss="modal" aria-label="Close">
					<span aria-hidden="true">&times;</span>
				</button>
			</div>
			<div class="modal-body">
				<form action="{{ page.fullURL.getURL() }}" method="post">
					<input type="hidden" name="itemID" value="{{ item.itemid }}">
					<input type="hidden" name="type" value="inspection">
					<input type="hidden" name="user" value="{{ user.loginid }}">
					<input type="hidden" name="rawnotetime" value="">

					<div class="row">
						<div class="col-6">
							<div class="form-group">
								<label for="inspection_note_date">Date</label>
								{% include 'util/date-picker.twig' with {'name': 'notedate', 'id': 'inspection_note_date', 'value': "now"|date("m/d/Y")} %}
							</div>
						</div>
						<div class="col-6">
							<div class="form-group">
								<label for="inspection_notetime">Time</label>
								<input type="text" class="form-control" name="notetime" id="inspection_notetime" value="{{ "now"|date("H:i") }}">
							</div>
						</div>
					</div>

					<div class="form-group">
						{% include 'msa/noce/ajax/button.twig' with {'input': '#inspection-notes'} %}
					</div>
					<div class="form-group">
						<label for="inspection-notes">Notes</label>
						{% include 'util/qnotes-textarea.twig' with {'id': 'inspection-notes', 'cols': qnotes.notes.inspection.get_column_count() - 1, 'rows': 15 } %}
					</div>

					<div class="d-flex justify-content-between align-items-center">
						<button type="submit" name="action" value="update-notes" class="btn btn-success">
							<i class="fa fa-floppy-o" aria-hidden="true"></i> Save
						</button>
						{# TODO: add confirmation for deletion 'delete_button' #}
						<button type="submit" name="action" value="delete-notes" class="btn btn-danger">
							<i class="fa fa-trash-o" aria-hidden="true"></i> Delete
						</button>
						<button type="button" class="btn btn-warning pull-right" data-dismiss="modal">
							Close
						</button>
					</div>
				</form>
			</div>
		</div>
	</div>
</div>
