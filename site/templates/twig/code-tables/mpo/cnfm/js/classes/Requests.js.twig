class CnfmRequests {
	static instance = null;

	static getInstance() {
		if (this.instance === null) {
			this.instance = new CnfmRequests();
		}
		return this.instance;
	}

	validateCode(code, callback) {
		var ajax = new AjaxRequest(this.validateCodeUrl());

		ajax.setData({code: code});
		ajax.request(function(response) {
			callback(response)
		});
	}

	getCode(code, callback) {
		var ajax = new AjaxRequest('{{ page.jsonApiUrl('mpo/cnfm/code/') }}');

		ajax.setData({code: code});
		ajax.request(function(response) {
			callback(response)
		});
	}

	validateCodeUrl() {
		return '{{ page.jsonApiUrl('mpo/validate/cnfm/') }}';
	}

}
