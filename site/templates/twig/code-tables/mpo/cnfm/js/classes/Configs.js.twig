class CnfmConfigs {
	static instance = null;

	static getInstance() {
		if (this.instance === null) {
			this.instance = new CnfmConfigs();
		}
		return this.instance;
	}

	constructor() {
		this.fields = {
			code: {
				maxlength: {{ cnfm.fieldAttribute('code', 'maxlength') }}
			},
			description: {
				maxlength: {{ cnfm.fieldAttribute('description', 'maxlength') }}
			},
		};
	}
}
