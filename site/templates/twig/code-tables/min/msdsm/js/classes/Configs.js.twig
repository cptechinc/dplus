class MsdsmConfigs {
	static instance = null;

	static getInstance() {
		if (this.instance === null) {
			this.instance = new MsdsmConfigs();
		}
		return this.instance;
	}

	constructor() {
		this.fields = {
			code: {
				maxlength: {{ manager.fieldAttribute('code', 'maxlength') }}
			},
			description: {
				maxlength: {{ manager.fieldAttribute('description', 'maxlength') }}
			},
			effectivedate: {
				dateformat: '{{ manager.fieldAttribute('effectivedate', 'dateformat') }}',
				momentformat: "MM/DD/Y"
			}
		};
	}
}
