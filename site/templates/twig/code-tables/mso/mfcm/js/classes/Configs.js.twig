class MfcmConfigs {
	static instance = null;

	static getInstance() {
		if (this.instance === null) {
			this.instance = new MfcmConfigs();
		}
		return this.instance;
	}

	constructor() {
		this.fields = {
			code: {
				maxlength: {{ mfcm.fieldAttribute('code', 'maxlength') }}
			},
			description: {
				maxlength: {{ mfcm.fieldAttribute('description', 'maxlength') }}
			},
		};
	}
}
