class DcmConfigs {
	static instance = null;

	static getInstance() {
		if (this.instance === null) {
			this.instance = new DcmConfigs();
		}
		return this.instance;
	}

	constructor() {
		this.fields = {
			code: {
				maxlength: {{ dcm.fieldAttribute('code', 'maxlength') }}
			},
			description: {
				maxlength: {{ dcm.fieldAttribute('description', 'maxlength') }}
			},
		};
	}
}
