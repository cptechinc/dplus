{#
	var                    Description / Instance of
	-------------------------------------------------
	input                  ProcessWire\WireInput
	manager                Dplus\Codes\Base
#}

{# data-code is for verifying edit vs new #}
<form action="{{ input.paginateUrl(1) }}" method="POST" id="code-form" data-code="">
	<input type="hidden" name="action" value="update">

	<div class="row">
		<div class="col-sm-2">
			<div class="form-group">
				<label for="code">Code</label>
				{% include 'util/bootstrap/form/input.twig' with {
						'input': {
							'name': 'code',
							'size': 'sm',
							'attributes': {
								'maxlength': manager.fieldAttribute('code', 'maxlength')
							}
						},
					}
				%}
			</div>
		</div>
		<div class="col-sm-6">
			<div class="form-group">
				<label for="description">Description</label>
				{% include 'util/bootstrap/form/input.twig' with {
						'input': {
							'name': 'description',
							'size': 'sm',
							'attributes': {
								'maxlength': manager.fieldAttribute('description', 'maxlength')
							}
						},
					}
				%}
			</div>
		</div>
	</div>

	{% include 'code-tables/mar/csv/form/ups-fields.twig' %}
	{% include 'code-tables/mar/csv/form/priority-shipping.twig' %}

	<div class="d-flex justify-content-between align-items-center">
		<button type="submit" class="btn btn-success form_button">
			<i class="fa fa-floppy-o" aria-hidden="true"></i> Save
		</button>
		<div>
			<a href="{{ page.codeDeleteUrl('') }}" class="btn btn-danger delete_button collapse">
				<i class="fa fa-trash" aria-hidden="true"></i> Delete
			</a>
		</div>
		<button type="button" class="btn btn-warning" data-dismiss="modal">
			<i class="fa fa-times" aria-hidden="true"></i> Close
		</button>
	</div>
</form>
