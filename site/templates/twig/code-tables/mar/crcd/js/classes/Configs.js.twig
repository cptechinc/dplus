class CrcdConfigs {
	static instance = null;

	static getInstance() {
		if (this.instance === null) {
			this.instance = new CrcdConfigs();
		}
		return this.instance;
	}

	constructor() {
		this.fields = {
			code: {
				maxlength: {{ manager.fieldAttribute('code', 'maxlength') }}
			},
			description: {
				maxlength: {{ manager.fieldAttribute('description', 'maxlength') }}
			},
			servicerate: {
				max: {{ manager.fieldAttribute('servicerate', 'max') }},
				precision: {{ manager.fieldAttribute('servicerate', 'precision') }},
			},
			transactioncost: {
				max: {{ manager.fieldAttribute('transactioncost', 'max') }},
				precision: {{ manager.fieldAttribute('transactioncost', 'precision') }},
			},
			surchargepercentcc: {
				max: {{ manager.fieldAttribute('surchargepercentcc', 'max') }},
				precision: {{ manager.fieldAttribute('surchargepercentcc', 'precision') }},
			},
		};
	}
}
