class CtmConfigs {
	static instance = null;

	static getInstance() {
		if (this.instance === null) {
			this.instance = new CtmConfigs();
		}
		return this.instance;
	}

	constructor() {
		this.fields = {
			code: {
				maxlength: {{ ctm.fieldAttribute('code', 'maxlength') }}
			},
			description: {
				maxlength: {{ ctm.fieldAttribute('description', 'maxlength') }}
			},
		};
	}
}
