{% set value = code.method == constant('METHOD_STD', manager) ? code.freightallow : 'N' %}

{% include 'util/bootstrap/form/select-simple.twig' with {
	'select': {
		'name': 'freightallow',
		'value' : value,
		'size': 'sm',
		'haskeys': true,
		'options': manager.fieldAttribute('freightallow', 'options'),
		'attributes': {
			'readonly': code.method == constant('METHOD_EOM', manager),
			'disabled': code.method == constant('METHOD_EOM', manager),
			'tabindex': code.method == constant('METHOD_EOM', manager) ? '-' ~ tabindex : tabindex,
		}
	}
} %}