class BumConfigs {
	static instance = null;

	static getInstance() {
		if (this.instance === null) {
			this.instance = new BumConfigs();
		}
		return this.instance;
	}

	constructor() {
		this.fields = {
			code: {
				maxlength: {{ bum.fieldAttribute('code', 'maxlength') }}
			},
			description: {
				maxlength: {{ bum.fieldAttribute('description', 'maxlength') }}
			},
		};
	}
}
