class CodeConfigs {
	static instance = null;

	static getInstance() {
		if (this.instance === null) {
			this.instance = new CodeConfigs();
		}
		return this.instance;
	}

	constructor() {
		this.fields = {
			code: {
				maxlength: {{ aoptm.fieldAttribute('code', 'maxlength') }}
			},
			description: {
				maxlength: {{ aoptm.fieldAttribute('description', 'maxlength') }}
			},
		};
	}
}
