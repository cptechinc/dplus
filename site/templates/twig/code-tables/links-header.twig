<div class="mb-3">
	{% if page.is_print() %}
		<button onclick="window.history.back();" class="btn btn-primary">
			<i class="fa fa-arrow-left" aria-hidden="true"></i> Back
		</button>
		&nbsp;
		<button class="btn btn-primary d-print-none" onclick="window.print();return false;">
			<i class="fa fa-print" aria-hidden="true"></i> Print
		</button>
	{% else %}
		<a href="{{ page.parent.url }}" class="btn btn-primary">
			<i class="fa fa-arrow-left" aria-hidden="true"></i> Back to {{ page.parent.title }}
		</a>
		&nbsp;

		{% if input.get.text('system') and input.get.text('code') %}
			<a href="{{ page.get_codetable_list_sysopURL(page.codetable, input.get.text('system'), input.get.text('code')) }}" class="btn btn-primary back-to-list">
				<i class="fa fa-arrow-left" aria-hidden="true"></i> Back to {{ page.title }}
			</a>
			&nbsp;
		{% elseif input.get.text('code') or input.get.text('sysop') %}
			<a href="{{ page.get_codetable_listURL(page.codetable, input.get.text('code')) }}" class="btn btn-primary back-to-list">
				<i class="fa fa-arrow-left" aria-hidden="true"></i> Back to {{ page.title }}
			</a>
			&nbsp;
		{% endif %}

		<a href="{{ page.get_printURL() }}" class="btn btn-primary">
			<i class="fa fa-print" aria-hidden="true"></i> View Printable
		</a>
	{% endif %}
</div>
