class LookupConfig {
	static instance = null;

	static getInstance() {
		if (this.instance === null) {
			this.instance = new LookupConfig();
		}
		return this.instance;
	}

	constructor() {
		this.custid = '{{ cart.getCustid() }}';
		this.shiptoid = '{{ cart.getShiptoid() }}';
	}

}
