{% extends "nav/default.twig" %}

{% block nav_links %}
	{% for menu in pages.get('/').children('template=warehouse-menu|menu') %}
		<li class="nav-item">
			<a class="nav-link" href="{{ menu.url }}">{{ menu.title }}</a>
		</li>
	{% endfor %}
{% endblock %}
