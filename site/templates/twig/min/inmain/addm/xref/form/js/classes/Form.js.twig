class AddmForm {
	static instance = null;

	static getInstance() {
		if (this.instance === null) {
			this.instance = new AddmForm();
		}
		return this.instance;
	}

	constructor() {
		this.id = 'i2i-form';
		this.inputs = AddmInputs.getInstance();
	}

	redirectExisting(goEdit) {
		if (goEdit === true) {
			var uri = URI();
			uri.setQuery('parentID', this.inputs.parentid.val());
			uri.setQuery('childtID', this.inputs.childid.val());
			window.location.href = uri.toString();
		} else {
			location.reload();
		}
	}

}
