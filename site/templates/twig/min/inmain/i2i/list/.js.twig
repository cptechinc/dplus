$(function() {
	var uri = URI();
	if (uri.hasQuery('focus')) {
		var query = uri.search(true);
		var focus = query.focus;

		if ($('tr[data-key="'+focus+'"]').length) {
			$('tr[data-key="'+focus+'"]').addClass('highlight');
			$('html, body').animate({scrollTop: $('[data-key="'+focus+'"]').offset().top,},700,'linear');
		}
	}
});
