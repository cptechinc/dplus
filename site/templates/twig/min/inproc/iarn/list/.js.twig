$(function() {
	var uri = URI();
	if (uri.hasQuery('focus')) {
		var query = uri.search(true);
		var focus = query.focus;

		if ($('tr[data-id="'+focus+'"]').length) {
			$('tr[data-id="'+focus+'"]').addClass('highlight');
			$('html, body').animate({scrollTop: $('[data-id="'+focus+'"]').offset().top,},700,'linear');
		}
	}
});
