class CmmConfigs {
	static instance = null;

	static getInstance() {
		if (this.instance === null) {
			this.instance = new CmmConfigs();
		}
		return this.instance;
	}

	constructor() {
		this.fields = {
			custid: {
				maxlength: {{ cmm.fieldAttribute('id', 'maxlength') }}
			},
			name: {
				maxlength: {{ cmm.fieldAttribute('name', 'maxlength') }}
			},
			country: {
				maxlength: {{ cmm.fieldAttribute('country', 'maxlength') }}
			},
			whseid: {
				maxlength: {{ cmm.fieldAttribute('whseid', 'maxlength') }}
			},
			salesperson1: {
				maxlength: {{ cmm.fieldAttribute('salesperson1', 'maxlength') }}
			},
			taxcode: {
				maxlength: {{ cmm.fieldAttribute('taxcode', 'maxlength') }}
			},
			termscode: {
				maxlength: {{ cmm.fieldAttribute('termscode', 'maxlength') }}
			},
			shipviacode: {
				maxlength: {{ cmm.fieldAttribute('shipviacode', 'maxlength') }}
			},
			typecode: {
				maxlength: {{ cmm.fieldAttribute('typecode', 'maxlength') }}
			},
			pricecode: {
				maxlength: {{ cmm.fieldAttribute('pricecode', 'maxlength') }}
			},
			commcode: {
				maxlength: {{ cmm.fieldAttribute('commcode', 'maxlength') }}
			},
		};
	}
}
