{#
	var                    Description / Instance of
	-------------------------------------------------
	page                   ProcessWire\Page
	input                  ProcessWire\WireInput
	optm                   Dplus\Ecrs\{Min\Ioptm | Map\Aoptm | Mar\Roptm| Mso\Soptm}
#}

class EcrRequests {
	static instance = null;

	constructor() {

	}

	static getInstance() {
		if (this.instance === null) {
			this.instance = new EcrRequests();
		}
		return this.instance;
	}


	getGlCode(code, callback) {
		var ajax = new AjaxRequest('{{ page.jsonApiUrl('mgl/gl-code/') }}');

		ajax.setData({code: code});
		ajax.request(function(response) {
			callback(response);
		});
	}

}
