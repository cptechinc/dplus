<div class="mb-3">
	<a href="{{ page.parent.url ~ '?qnbr=' ~ qnbr }}" class="btn btn-primary">
		<i class="fa fa-arrow-left" aria-hidden="true"></i> Go back to Quote
	</a>
</div>

<div class="mb-4">
	<div class="row mb-1">
		<div class="col">
			<h4>Header Notes</h4>
		</div>
		<div class="col">
			<button type="button" class="btn btn-primary" data-toggle="modal" data-target="#add-note-modal" data-linenbr="0" data-sequence="0">
				<i class="fa fa-plus" aria-hidden="true"></i> Add Note
			</button>
		</div>
	</div>
	{% include 'quotes/quote/notes/qnotes-list.twig' with {'notes': quote.get_notes() } %}
</div>

{% for item in items %}
	<div class="row mb-1">
		<div class="col">
			<h4>Line Number {{ item.linenbr ~ ': ' ~ item.itemid }}</h4>
		</div>
		<div class="col">
			<button type="button" class="btn btn-primary" data-toggle="modal" data-target="#add-note-modal" data-linenbr="{{ item.linenbr }}" data-sequence="0">
				<i class="fa fa-plus" aria-hidden="true"></i> Add Note
			</button>
		</div>
	</div>
	<div class="mb-3">
		{% include 'quotes/quote/notes/qnotes-list.twig' with {'notes': item.get_notes() } %}
	</div>
{% endfor %}
