<nav class="toolbar-sidebar">
	<ul class="nav">
		{% for key, data in page.viPermittedSubfunctions() %}
			<li>
				<a href="{{ page.viSubfunctionUrl(vendorID, key) }}">
					{{ data.title ? data.title : key|capitalize }}
				</a>
			</li>
		{% endfor %}
	</ul>
</nav>
