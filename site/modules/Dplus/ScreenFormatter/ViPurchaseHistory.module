<?php namespace ProcessWire;

/**
 * Class for formatting the VI Purchase History Screen
 */
class ViPurchaseHistory extends ScreenFormatter implements Module {
	protected $code = 'vi-purchase-history';

	/**
	 * Key Value array of Sections that exist I.E. header => Header, detail => Detail
	 * @var string
	 */
	protected $datasections = array(
		"detail" => "Detail"
	);

	/* =============================================================
		ProcessWire Module Functions
	============================================================= */
		/**
		 * ProcessWire Module Info
		 *
		 * @return void
		 */
		public static function getModuleInfo() {
			return array(
				'title' => 'Dpluso VI Purchase History Formatter Module',
				'version' => 101,
				'summary' => 'Handles VI Purchase History Formatter',
				'singular' => true,
				'autoload' => true,
			);
		}
}
